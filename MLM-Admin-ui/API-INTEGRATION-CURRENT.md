## MLM-Admin-ui – Live API Integration Checklist (Code-Based)

This file is generated by scanning the actual React pages in `src/app` and their imports from `src/lib/api`.  
Only pages that call real backend APIs are listed here.

---

## Authentication & Settings

- ✅ `/login` → `lib/api/auth.ts` (`adminLogin`) → `POST /api/v1/auth/admin/login`
- ✅ `/settings1` (Update Password) → `lib/api/auth.ts` (`changePassword`) → `PUT /api/v1/profile/password`

---

## Dashboard

- ✅ `/dashboard` → `lib/api/dashboard.ts` (`getDashboard`)  
  - Uses: `GET /api/v1/admin/dashboard` and additional admin endpoints (reports, users, purchases) for extra stats

---

## User Management

- ✅ `/user-management/users-summary` → `lib/api/users.ts` (`getUsers`)  
  - Uses: `GET /api/v1/admin/users`
- ✅ `/user-management/users-details` → `lib/api/users.ts` (`getUsers`, `getUserById`, `updateUser`, `deleteUser`, `activateUser`, `deactivateUser`)  
  - Uses:  
    - `GET /api/v1/admin/users`  
    - `GET /api/v1/admin/users/:id`  
    - `PUT /api/v1/admin/users/:id`  
    - `DELETE /api/v1/admin/users/:id`  
    - `POST /api/v1/admin/users/:id/activate`  
    - `POST /api/v1/admin/users/:id/deactivate`
- ✅ `/user-management/users-wallet` → `lib/api/users.ts` (`getUsers`, `manualCreditWallet`, `manualDebitWallet`)  
  - Uses:  
    - `GET /api/v1/admin/users`  
    - `POST /api/v1/admin/commissions/manual-credit`  
    - `POST /api/v1/admin/commissions/manual-debit`
- ✅ `/user-management/users-address` → `lib/api/kyc.ts` (`getAllProfiles`)  
  - Uses: `GET /api/v1/admin/profiles`
- ✅ `/user-management/users-kyc` → `lib/api/kyc.ts` (`getAllProfiles`, `getPendingKYCs`, `updateKYCStatus`)  
  - Uses:  
    - `GET /api/v1/admin/profiles`  
    - `GET /api/v1/admin/kyc/pending`  
    - `PUT /api/v1/admin/kyc/:user_id/update`
- ✅ `/user-management/manual-kyc` → `lib/api/kyc.ts`, `lib/api/users.ts` (`getUserDocuments`, `approveKYC`, `rejectKYC`, `getUsers`)  
  - Uses:  
    - `GET /api/v1/admin/kyc/:user_id/documents`  
    - `POST /api/v1/admin/kyc/:user_id/approve`  
    - `POST /api/v1/admin/kyc/:user_id/reject`  
    - `GET /api/v1/admin/users`

---

## Withdrawals & P2P

- ✅ `/withdraw/pending-withdraw` → `lib/api/withdraw.ts` (`getPendingWithdrawals`, `approveWithdrawal`, `rejectWithdrawal`)  
  - Uses:  
    - `GET /api/v1/admin/withdraw/pending`  
    - `POST /api/v1/admin/withdraw/requests/:id/approve`  
    - `POST /api/v1/admin/withdraw/requests/:id/reject`
- ✅ `/withdraw/withdraw-history` → `lib/api/withdraw.ts` (`getWithdrawalHistory`)  
  - Uses: `GET /api/v1/admin/withdraw/history`
- ✅ `/p2p-history` → `lib/api/withdraw.ts` (`getWalletTransfers`)  
  - Uses: `GET /api/v1/admin/wallet/transfers`

---

## Income History

- ✅ `/income-history/self-income` → `lib/api/incomeHistory.ts` (`getSelfIncome`)  
  - Uses: `GET /api/v1/income-history/self-income`
- ✅ `/income-history/direct-income` → `lib/api/incomeHistory.ts` (`getDirectIncome`)  
  - Uses: `GET /api/v1/income-history/direct-income`
- ✅ `/income-history/team-income` → `lib/api/incomeHistory.ts` (`getTeamIncome`)  
  - Uses: `GET /api/v1/income-history/team-income`
- ✅ `/income-history/pyramid-income` → `lib/api/incomeHistory.ts` (`getSpotIncome`)  
  - Uses: `GET /api/v1/income-history/spot-income`
- ✅ `/income-history/spot-commision` → `lib/api/ledger.ts` (`getLedgerEntries`)  
  - Uses: `GET /api/v1/admin/audit-log` (filtered for SPOT commissions)

---

## Master Settings

- ✅ `/master/levels` → `lib/api/levels.ts` (`getLevels`, `getLevelDetails`, `updateLevel`)  
  - Uses:  
    - `GET /api/v1/admin/levels`  
    - `GET /api/v1/admin/levels/:level`  
    - `PUT /api/v1/admin/levels/:level`
- ✅ `/master/fee-rules` → `lib/api/feeRules.ts` (`getFeeRules`, `getFeeRuleById`, `createFeeRule`, `updateFeeRule`, `deleteFeeRule`, `getFeeTransactions`, `getUserFeeTransactions`)  
  - Uses:  
    - `GET /api/v1/admin/fees/rules`  
    - `GET /api/v1/admin/fees/rules/:id`  
    - `POST /api/v1/admin/fees/rules`  
    - `PUT /api/v1/admin/fees/rules/:id`  
    - `DELETE /api/v1/admin/fees/rules/:id`  
    - `GET /api/v1/admin/fees/transactions`  
    - `GET /api/v1/admin/fees/transactions/:userId`
- ✅ `/master/package-setting` → `lib/api/packages.ts` (`getPackages`, `getPackageById`, `createPackage`, `updatePackage`, `deletePackage`)  
  - Uses:  
    - `GET /api/v1/admin/packages`  
    - `GET /api/v1/admin/packages/:id`  
    - `POST /api/v1/admin/packages`  
    - `PUT /api/v1/admin/packages/:id`  
    - `DELETE /api/v1/admin/packages/:id`
- ✅ `/master/company-bank` → `lib/api/companyBank.ts` (`getCompanyBanks`, `getCompanyBankById`, `createCompanyBank`, `updateCompanyBank`, `deleteCompanyBank`)  
  - Uses:  
    - `GET /api/v1/admin/company-bank`  
    - `GET /api/v1/admin/company-bank/:id`  
    - `POST /api/v1/admin/company-bank`  
    - `PUT /api/v1/admin/company-bank/:id`  
    - `DELETE /api/v1/admin/company-bank/:id`
- ✅ `/master/amount-setup` → `lib/api/withdrawalTransferRules.ts` (`getWithdrawalTransferRules`, `updateWithdrawalTransferRules`)  
  - Uses:  
    - `GET /api/v1/admin/withdrawal-transfer-rules`  
    - `PUT /api/v1/admin/withdrawal-transfer-rules`
- ✅ `/master/notice-board` → `lib/api/notices.ts` (`getNotices`, `getNoticeById`, `createNotice`, `updateNotice`, `deleteNotice`)  
  - Uses:  
    - `GET /api/v1/admin/notices`  
    - `GET /api/v1/admin/notices/:id`  
    - `POST /api/v1/admin/notices`  
    - `PUT /api/v1/admin/notices/:id`  
    - `DELETE /api/v1/admin/notices/:id`

---

## Website Settings

- ✅ `/website-setting/landing-slider` → `lib/api/website.ts` (`getSliders`, `createSlider`, `updateSlider`, `deleteSlider`)  
  - Uses:  
    - `GET /api/v1/admin/website/slider`  
    - `POST /api/v1/admin/website/slider`  
    - `PUT /api/v1/admin/website/slider/:id`  
    - `DELETE /api/v1/admin/website/slider/:id`
- ✅ `/website-setting/notice` → `lib/api/website.ts` (`getWebsiteNotices`, `createWebsiteNotice`)  
  - Uses:  
    - `GET /api/v1/admin/website/notices`  
    - `POST /api/v1/admin/website/notices`  
  - Note: Backend does not expose PUT/DELETE for website notices; UI intentionally does not provide edit/delete.

---

## Courses (Admin)

- ✅ `/course-master/course-module` → `lib/api/courses.ts`, `lib/api/packages.ts`  
  - Uses:  
    - `GET /api/v1/admin/courses`  
    - `GET /api/v1/admin/courses/:id`  
    - `POST /api/v1/admin/courses`  
    - `PUT /api/v1/admin/courses/:id`  
    - `DELETE /api/v1/admin/courses/:id`  
    - `GET /api/v1/admin/packages`
- ✅ `/course-master/course-vedios` → `lib/api/courses.ts` (`getAllVideos`, `getCourses`, `getCourseById`, `createModule`, `createVideo`, `updateVideo`, `deleteVideo`)  
  - Uses course and module/video endpoints under `/api/v1/admin/courses`

---

## Ledger & Reports

- ✅ `/ledger-logs` → `lib/api/ledger.ts` (`getLedgerEntries`, `getCommissionBreakdown`)  
  - Uses:  
    - `GET /api/v1/admin/audit-log`  
    - `GET /api/v1/admin/reports/commission-breakdown`

---

## Notifications

- ✅ `/notification-module` → `lib/api/notices.ts` (`getNotices`, `createNotice`, `updateNotice`, `deleteNotice`)  
  - Uses: `/api/v1/admin/notices` (same endpoints as master notice-board, focused on admin notifications list/create/update/delete)

---

## Pages Without Backend API Calls

The following pages do **not** currently call any backend APIs (pure UI/placeholder logic only):

- `/` (root dashboard wrapper, if present)
- `/login` layout shell (aside from `adminLogin` call described above)
- `/logout` (client-side session clear + redirect)
- Any route under `src/app` that does not import from `../../lib/api/*` and does not use `fetch`.


